# Defaults for jenkins swarm slave

# USER=jenkins
# HOME="/var/lib/jenkins"
# JAR="${HOME}/swarm-slave.jar"
# LOG="/var/log/${NAME}.log"
# USERNAME=<%= node['ws-jenkins']['slave']['jenkins_ui_user'] %>
# PASSWORD=<%= node['ws-jenkins']['slave']['jenkins_ui_password'] %>
# FSROOT=${HOME}

JAVA_ARGS="<%= node['ws-jenkins']['slave']['jvm_options'] %>"
DESCRIPTION="Jenkins Swarm Slave Description missing.."
MASTER=<%= node['ws-jenkins']['slave']['master_url'] %>
EXECUTORS=<%= node['ws-jenkins']['slave']['executors'] %>
LABELS="Swarm <%= node['ws-jenkins']['slave']['labels'] %>"
CLIENTNAME="<%= node['ws-jenkins']['slave']['name'] %>"
